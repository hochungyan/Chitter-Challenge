{
  "name": "server",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "SET NODE_ENV=test&& mocha --timeout=10000 --exit",
    "start": "SET NODE_ENV=dev&&nodemon server",
    "build": "cross-env NODE_ENV=dev node server.js"
  },
  "dependencies": {
    "axios": "^1.2.0",
    "chai-http": "^4.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mocha": "^10.1.0",
    "mongoose": "^6.8.0",
    "nodemon": "^2.0.20",
    "router": "^1.3.7"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "cross-env": "^7.0.3"
  }
}
